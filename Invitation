<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Zul & Partner Wedding</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <!-- CSS -->
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- HERO SECTION -->
  <section class="hero">
    <h1>Zul & Partner</h1>
    <p class="subtitle">Kami menjemput anda ke majlis perkahwinan kami</p>
    <p class="date">20 July 2026</p>
    <div id="countdown"></div>
  </section>

  <!-- EVENT DETAILS -->
  <section class="card">
    <h2>📍 Majlis Details</h2>
    <p><strong>Nikah:</strong> 10:00 AM</p>
    <p><strong>Reception:</strong> 12:00 PM – 4:00 PM</p>
    <p><strong>Venue:</strong> Dewan Seri Indah, Selangor</p>
  </section>

  <!-- LOCATION -->
  <section class="card">
    <h2>🗺 Location</h2>
    <iframe 
      src="https://www.google.com/maps?q=selangor&output=embed"
      loading="lazy">
    </iframe>
  </section>

  <!-- RSVP -->
  <section class="card">
    <h2>📩 RSVP</h2>
    <p>Sila sahkan kehadiran anda</p>
    <a class="btn" href="https://forms.gle/PUT_YOUR_FORM_LINK" target="_blank">
      RSVP Now
    </a>
  </section>

  <!-- GALLERY -->
  <section class="card">
    <h2>💍 Our Moments</h2>
    <img src="images/couple.jpg" alt="Couple Photo">
  </section>

  <!-- FOOTER -->
  <footer>
    <p>With love ❤️ Zul & Partner</p>
  </footer>

  <!-- JS -->
  <script src="script.js"></script>
</body>
</html>const weddingDate = new Date("July 20, 2026 10:00:00").getTime();

const timer = setInterval(() => {
  const now = new Date().getTime();
  const distance = weddingDate - now;

  if (distance < 0) {
    document.getElementById("countdown").innerHTML = "Hari Bahagia Telah Tiba 💕";
    clearInterval(timer);
    return;
  }

  const days = Math.floor(distance / (1000 * 60 * 60 * 24));
  const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
  const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
  const seconds = Math.floor((distance % (1000 * 60)) / 1000);

  document.getElementById("countdown").innerHTML =
    `${days} days ${hours} hrs ${minutes} mins ${seconds} secs`;
}, 1000);
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}

body {
  background: #fdf6f8;
  color: #444;
}

/* HERO */
.hero {
  background: linear-gradient(135deg, #f8cdda, #d1e8e2);
  text-align: center;
  padding: 80px 20px;
}

.hero h1 {
  font-size: 3rem;
  color: #5b3c4d;
}

.subtitle {
  margin-top: 10px;
}

.date {
  margin-top: 15px;
  font-weight: 600;
}

#countdown {
  margin-top: 20px;
  font-size: 1.2rem;
}

/* CARD */
.card {
  background: #fff;
  margin: 30px auto;
  padding: 30px;
  max-width: 600px;
  border-radius: 20px;
  text-align: center;
  box-shadow: 0 10px 25px rgba(0,0,0,0.08);
}

.card h2 {
  margin-bottom: 15px;
  color: #7a4a5a;
}

.card iframe {
  width: 100%;
  height: 300px;
  border-radius: 15px;
  border: none;
}

.card img {
  width: 100%;
  border-radius: 15px;
}

/* BUTTON */
.btn {
  display: inline-block;
  padding: 12px 25px;
  background: #e6a4b4;
  color: #fff;
  border-radius: 30px;
  text-decoration: none;
}

.btn:hover {
  background: #d48fa0;
}

/* FOOTER */
footer {
  text-align: center;
  padding: 30px;
}

